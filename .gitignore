node_modules/
dist/
*.log

# the built installer (ITERA-Setup.exe) is committed via Git LFS so it can be
# downloaded straight from the repo — see .gitattributes

# OS / editor
Thumbs.db
.DS_Store
.vscode/

# local render/test artifacts
_*.png
_*.js
